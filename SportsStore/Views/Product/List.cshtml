@model IEnumerable<Product>



@if (Model == null) {
    <h2>Model Is null</h2>
}
else {
    @foreach (var product in Model) {
        <div>
            <h3>@product.Name</h3>
            @product.Description
            <h4>@product.Price.ToString("C")</h4>
        </div>
    }
}